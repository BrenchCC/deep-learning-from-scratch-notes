---
author: Brench
date: 2026-07-02
date-modified: 2026-07-02
open-graph:
  title: chapter 3.6-train-mlp-on-mnist
  description: "3.6 用 NumPy 与 PyTorch 在 MNIST 上训练 MLP"
title: "Chapter 3.6 用 NumPy 与 PyTorch 在 MNIST 上训练 MLP"
order: 6
---

前面一节分别用 NumPy 和 PyTorch 搭建了两层 MLP，但测试数据来自随机生成的小规模数组，只能验证前向传播、反向传播和参数更新是否连通。本节把同一个模型放到 MNIST 上，完成数据读取、mini-batch 训练、测试集评估和预测结果检查。

NumPy 版本保留手写训练逻辑，只借助 TorchVision 下载数据；PyTorch 版本则使用 `Dataset`、`DataLoader`、`nn.Module`、autograd 和优化器完成同一任务。两套实现遵循相同的计算顺序：

```text
load data -> mini-batch -> forward -> loss -> backward -> update -> evaluate
```

对照这两套代码，可以看清 PyTorch 自动处理了哪些工作，也能避免把框架接口误解为一套不同的训练算法。

```{python}
import os
import sys
import math
from collections import defaultdict

import torch
import numpy as np
import matplotlib.pyplot as plt
import torchvision.datasets as datasets
from torch import nn
from torchvision import transforms
from torch.utils.data import DataLoader as TorchDataLoader

# Add project root to Python path
sys.path.append(os.getcwd())

import deep_learning
import deep_learning.models.mlp as mlp

rng = np.random.default_rng(42)
print('NumPy version:', np.__version__)
print('PyTorch version:', torch.__version__)
```

## 3.6.1 准备 MNIST 数据集

MNIST 包含 $28 \times 28$ 的灰度手写数字图像，标签取值为 0 到 9。MLP 不显式利用二维空间结构，因此需要把每张图像展平成 784 维向量：

```math
28 \times 28 = 784
```

一个 batch 的输入形状为：

```math
X \in \mathbb{R}^{B \times 784}
```

其中，$B$ 是 batch size。先用 TorchVision 下载训练集和测试集：

```{python}
root = deep_learning.get_data_root()
train_ds = datasets.MNIST(root, train = True, download = True)
test_ds = datasets.MNIST(root, train = False, download = True)
```

TorchVision 把图像和标签分别保存在 `data` 与 `targets` 属性中。NumPy 版本直接读取这两个 tensor，再完成展平和归一化：

```{python}
X_train = train_ds.data.numpy()
X_train = np.reshape(X_train, (-1, 28 * 28))
X_train = X_train.astype(np.float32) / 255.0
y_train = train_ds.targets.numpy()

X_test = test_ds.data.numpy()
X_test = np.reshape(X_test, (-1, 28 * 28))
X_test = X_test.astype(np.float32) / 255.0
y_test = test_ds.targets.numpy()

print('X_train shape:', X_train.shape)
print('y_train shape:', y_train.shape)
print('X_test shape:', X_test.shape)
print('y_test shape:', y_test.shape)
```

除以 255.0 后，像素范围从 $[0, 255]$ 变为 $[0, 1]$。这一步不会改变图像内容，但能限制输入尺度，减少线性层 logits 因像素值过大而剧烈波动的情况。

下面恢复几张图像，确认展平操作没有打乱像素顺序：

```{python}
fig = plt.figure(1, figsize = (8, 2))
axes = fig.subplots(1, 6)
for i, ax in enumerate(axes.ravel()):
    ax.imshow(X_train[i].reshape(28, 28), cmap = 'gray')
    ax.axis('off')
    ax.set_title(f'label: {y_train[i]}', fontsize = 10)
plt.show()
```

展平只改变输入表示。MLP 接收普通向量，不知道哪些像素在原图中相邻；这一限制会在本节末尾进一步讨论。

## 3.6.2 NumPy 的 mini-batch 迭代器

一次参数更新只使用一个 mini-batch：

```math
X_{\text{batch}} \in \mathbb{R}^{B \times 784},
\qquad
y_{\text{batch}} \in \mathbb{R}^{B}
```

相比整批训练，mini-batch 降低了单次更新的内存和计算量；每个 batch 的样本组成也会改变，使梯度带有一定随机性。下面的迭代器在每个 epoch 开始时打乱索引，再按 `batch_size` 返回数据：

```{python}
class NumPyDataLoader:
    def __init__(
        self,
        X: np.ndarray,
        y: np.ndarray,
        batch_size: int,
        shuffle: bool = True,
    ):
        """Initialize a NumPy mini-batch iterator.

        Args:
            X: Input samples with the batch dimension first.
            y: Integer class labels aligned with ``X``.
            batch_size: Maximum number of samples in each batch.
            shuffle: Whether to shuffle sample indices before each epoch.
        """
        self.X = X
        self.y = y
        self.batch_size = batch_size
        self.shuffle = shuffle

    def __iter__(self):
        """Yield one epoch of mini-batches."""
        indices = np.arange(len(self.X))

        if self.shuffle:
            rng.shuffle(indices)

        for start in range(0, len(self.X), self.batch_size):
            idx = indices[start : start + self.batch_size]
            yield self.X[idx], self.y[idx]

    def __len__(self):
        """Return the number of mini-batches in one epoch."""
        return math.ceil(len(self.X) / self.batch_size)
```

最后一个 batch 可能少于 `batch_size`，因此 `__len__` 使用向上取整。训练损失会按实际 batch 大小加权，避免最后一个小 batch 与完整 batch 获得相同权重。

## 3.6.3 从 logits 计算准确率

模型对每张图像输出 10 个 logits：

```math
Z \in \mathbb{R}^{B \times 10}
```

第 $i$ 个样本的预测类别是该行最大 logit 对应的索引：

```math
\hat{y}_i = \arg\max_j Z_{i,j}
```

对应的 NumPy 实现为：

```{python}
def accuracy(logits: np.ndarray, labels: np.ndarray) -> float:
    """Compute classification accuracy from logits.

    Args:
        logits: Class scores with shape ``(batch_size, num_classes)``.
        labels: Integer class labels with shape ``(batch_size,)``.

    Returns:
        The fraction of correctly classified samples.
    """
    predictions = np.argmax(logits, axis = 1)
    return float(np.mean(predictions == labels))
```

Softmax 会改变数值尺度，但不会改变类别之间的大小顺序。只计算预测类别时，直接对 logits 使用 `argmax` 即可，不需要额外执行 softmax。

## 3.6.4 用 NumPy 训练 MLP

先准备数据迭代器、模型、损失函数和 SGD 优化器：

```{python}
train_dl = NumPyDataLoader(
    X_train,
    y_train,
    batch_size = 128,
    shuffle = True,
)
test_dl = NumPyDataLoader(
    X_test,
    y_test,
    batch_size = 128,
    shuffle = False,
)

model = mlp.MLP(input_dim = 784, hidden_dim = 256, num_classes = 10)
loss_fn = mlp.CrossEntropyLoss()
optimizer = mlp.SGD(model.parameters(), lr = 0.1)

history = defaultdict(list)
```

每个 mini-batch 依次执行 `forward -> loss -> backward -> update`。测试阶段只做前向计算，不调用 `backward()` 和 `step()`：

```{python}
num_epochs = 10

for epoch in range(1, num_epochs + 1):
    train_loss = 0.0
    correct_samples = 0.0

    for X, y in train_dl:
        logits = model(X)
        loss = loss_fn(logits, y)

        dlogits = loss_fn.backward()
        model.backward(dlogits)

        optimizer.step()
        optimizer.zero_grad()

        train_loss += loss.item() * len(y)
        correct_samples += accuracy(logits, y) * len(y)

    avg_train_loss = train_loss / len(train_ds)
    avg_train_acc = correct_samples / len(train_ds)
    history['loss'].append(avg_train_loss)
    history['acc'].append(avg_train_acc)

    test_loss = 0.0
    correct_samples = 0.0

    for X, y in test_dl:
        logits = model(X)
        loss = loss_fn(logits, y)

        test_loss += loss.item() * len(y)
        correct_samples += accuracy(logits, y) * len(y)

    avg_test_loss = test_loss / len(test_ds)
    avg_test_acc = correct_samples / len(test_ds)
    history['test_loss'].append(avg_test_loss)
    history['test_acc'].append(avg_test_acc)

    width = len(str(num_epochs))
    print(
        f'Epoch [{epoch:{width}d}/{num_epochs:{width}d}] '
        f'| loss: {avg_train_loss:.4f} '
        f'| acc: {avg_train_acc:.4f} '
        f'| test_loss: {avg_test_loss:.4f} '
        f'| test_acc: {avg_test_acc:.4f}'
    )
```

这里的 `loss_fn.backward()` 给出损失关于 logits 的梯度，`model.backward(dlogits)` 再把梯度依次传过第二个线性层、ReLU 和第一个线性层。`optimizer.step()` 更新参数后，`zero_grad()` 清空本次梯度，为下一个 batch 做准备。

## 3.6.5 用 PyTorch 表示数据和模型

PyTorch 版本不需要先把整个数据集转换为 NumPy 数组。`transforms.ToTensor()` 会把 PIL 图像转换为形状为 $(1, 28, 28)$ 的 `float32` tensor，并把像素缩放到 $[0, 1]$。`TorchDataLoader` 负责打乱样本、组织 batch 和迭代数据：

```{python}
transform = transforms.ToTensor()
torch_train_ds = datasets.MNIST(
    root,
    train = True,
    download = True,
    transform = transform,
)
torch_test_ds = datasets.MNIST(
    root,
    train = False,
    download = True,
    transform = transform,
)

torch_train_dl = TorchDataLoader(torch_train_ds, batch_size = 128, shuffle = True)
torch_test_dl = TorchDataLoader(torch_test_ds, batch_size = 128, shuffle = False)
```

模型结构与 NumPy 版本一致。`nn.Flatten()` 在 batch 维之后展平图像，两个 `nn.Linear` 之间插入 ReLU：

```{python}
torch_model = nn.Sequential(
    nn.Flatten(),
    nn.Linear(784, 256),
    nn.ReLU(),
    nn.Linear(256, 10),
)

if torch.cuda.is_available():
    device = torch.device('cuda')
elif torch.backends.mps.is_available():
    device = torch.device('mps')
else:
    device = torch.device('cpu')

torch_model = torch_model.to(device)
torch_loss_fn = nn.CrossEntropyLoss()
torch_optimizer = torch.optim.SGD(torch_model.parameters(), lr = 0.1)
torch_history = defaultdict(list)

print('Training device:', device)
```

`nn.Linear(784, 256)` 与手写 `mlp.Linear` 表示相同的映射，但权重存储方向不同：NumPy 实现使用形状 $(784, 256)$ 并计算 $XW+b$，PyTorch 使用形状 $(256, 784)$ 并计算 $XW^\top+b$。

## 3.6.6 用 PyTorch 训练和评估 MLP

PyTorch 训练循环的数学过程没有变化。主要差别是 `loss.backward()` 由 autograd 沿计算图求梯度，参数更新由 `torch.optim.SGD` 完成：

```{python}
for epoch in range(1, num_epochs + 1):
    torch_model.train()
    train_loss = 0.0
    correct_samples = 0

    for images, labels in torch_train_dl:
        images = images.to(device)
        labels = labels.to(device)

        torch_optimizer.zero_grad()
        logits = torch_model(images)
        loss = torch_loss_fn(logits, labels)
        loss.backward()
        torch_optimizer.step()

        train_loss += loss.item() * len(labels)
        correct_samples += (logits.argmax(dim = 1) == labels).sum().item()

    avg_train_loss = train_loss / len(torch_train_ds)
    avg_train_acc = correct_samples / len(torch_train_ds)
    torch_history['loss'].append(avg_train_loss)
    torch_history['acc'].append(avg_train_acc)

    torch_model.eval()
    test_loss = 0.0
    correct_samples = 0

    with torch.inference_mode():
        for images, labels in torch_test_dl:
            images = images.to(device)
            labels = labels.to(device)

            logits = torch_model(images)
            loss = torch_loss_fn(logits, labels)

            test_loss += loss.item() * len(labels)
            correct_samples += (logits.argmax(dim = 1) == labels).sum().item()

    avg_test_loss = test_loss / len(torch_test_ds)
    avg_test_acc = correct_samples / len(torch_test_ds)
    torch_history['test_loss'].append(avg_test_loss)
    torch_history['test_acc'].append(avg_test_acc)

    width = len(str(num_epochs))
    print(
        f'Epoch [{epoch:{width}d}/{num_epochs:{width}d}] '
        f'| loss: {avg_train_loss:.4f} '
        f'| acc: {avg_train_acc:.4f} '
        f'| test_loss: {avg_test_loss:.4f} '
        f'| test_acc: {avg_test_acc:.4f}'
    )
```

这段代码中有三个容易遗漏的状态管理操作：

- `torch_model.train()` 启用训练模式，`torch_model.eval()` 切换到评估模式。当前 MLP 没有 Dropout 或 BatchNorm，因此两种模式的输出相同；显式切换仍然是可迁移到其他模型的正确写法。
- PyTorch 默认累积参数梯度，所以每个训练步骤都要调用 `torch_optimizer.zero_grad()`。把它放在前向传播之前，能清楚地表示「本 batch 从空梯度开始」。
- `torch.inference_mode()` 在测试阶段关闭梯度记录，减少计算图带来的内存和调度开销。

## 3.6.7 两套训练循环的对应关系

NumPy 与 PyTorch 版本的核心步骤可以逐项对应：

| 训练环节 | NumPy 手写实现 | PyTorch 实现 |
|---|---|---|
| 组织 batch | `NumPyDataLoader` | `torch.utils.data.DataLoader` |
| 模型前向传播 | `logits = model(X)` | `logits = torch_model(images)` |
| 计算损失 | `loss_fn(logits, y)` | `torch_loss_fn(logits, labels)` |
| 反向传播 | `loss_fn.backward()` 与 `model.backward()` | `loss.backward()` |
| 参数更新 | `optimizer.step()` | `torch_optimizer.step()` |
| 清空梯度 | `optimizer.zero_grad()` | `torch_optimizer.zero_grad()` |
| 评估上下文 | 不调用手写 `backward()` | `eval()` 与 `inference_mode()` |

PyTorch 省去的是梯度公式和参数遍历的显式代码，并没有改变梯度下降的步骤。手写实现适合检查每一层的局部梯度，框架实现则更适合扩展模型、切换设备和组合标准组件。

## 3.6.8 观察训练曲线

把两套实现的 loss 和 accuracy 画在同一组坐标轴上，可以检查它们是否呈现相近的优化趋势：

```{python}
epochs = np.arange(1, num_epochs + 1)
fig, axes = plt.subplots(1, 2, figsize = (12, 4))

axes[0].plot(
    epochs,
    history['loss'],
    marker = 'o',
    label = 'NumPy train',
)
axes[0].plot(
    epochs,
    history['test_loss'],
    marker = 'o',
    label = 'NumPy test',
)
axes[0].plot(
    epochs,
    torch_history['loss'],
    linestyle = '--',
    label = 'PyTorch train',
)
axes[0].plot(
    epochs,
    torch_history['test_loss'],
    linestyle = '--',
    label = 'PyTorch test',
)
axes[0].set_xlabel('Epoch')
axes[0].set_ylabel('Loss')
axes[0].set_title('MLP Training Loss')
axes[0].grid(linestyle = '--', alpha = 0.7)
axes[0].legend()

axes[1].plot(
    epochs,
    history['acc'],
    marker = 'o',
    label = 'NumPy train',
)
axes[1].plot(
    epochs,
    history['test_acc'],
    marker = 'o',
    label = 'NumPy test',
)
axes[1].plot(
    epochs,
    torch_history['acc'],
    linestyle = '--',
    label = 'PyTorch train',
)
axes[1].plot(
    epochs,
    torch_history['test_acc'],
    linestyle = '--',
    label = 'PyTorch test',
)
axes[1].set_xlabel('Epoch')
axes[1].set_ylabel('Accuracy')
axes[1].set_title('MLP Training Accuracy')
axes[1].grid(linestyle = '--', alpha = 0.7)
axes[1].legend()

fig.tight_layout()
plt.show()
```

两条曲线不必逐点重合。两套模型的参数初始化、batch 打乱顺序和底层数值计算都可能不同；合理的验证目标是 loss 持续下降且测试准确率随训练提升。如果需要严格比较实现，应固定相同初始参数和 batch 顺序，再逐步比较 logits、loss 与梯度。

## 3.6.9 查看预测结果

从测试集中抽取相同样本，分别查看 NumPy 与 PyTorch 模型的预测：

```{python}
indices = rng.choice(len(X_test), size = 10, replace = False)
x_samples = X_test[indices]
y_samples = y_test[indices]

numpy_logits = model(x_samples)
numpy_predictions = np.argmax(numpy_logits, axis = 1)

torch_model.eval()
with torch.inference_mode():
    torch_samples = torch.from_numpy(x_samples).reshape(
        -1,
        1,
        28,
        28,
    ).to(device)
    torch_predictions = torch_model(torch_samples).argmax(dim = 1).cpu().numpy()
```

```{python}
fig = plt.figure(3, figsize = (10, 4))
axes = fig.subplots(2, 5)
for i, ax in enumerate(axes.ravel()):
    ax.imshow(x_samples[i].reshape(28, 28), cmap = 'gray')
    ax.axis('off')
    ax.set_title(
        f'NumPy: {numpy_predictions[i]}\n'
        f'PyTorch: {torch_predictions[i]}\n'
        f'Label: {y_samples[i]}',
        fontsize = 9,
    )
fig.tight_layout()
plt.show()
```

样本级结果比单个 accuracy 数字更容易暴露失败模式，例如笔画粘连、数字书写倾斜或类别形状相似。这里只做定性检查；要比较两个实现的泛化能力，仍应以完整测试集指标为准。

## 3.6.10 MLP 在图像任务上的局限

MLP 把二维图像展平成普通向量：

```math
X = [x_1, x_2, \ldots, x_{784}]
```

模型不会显式利用局部区域、平移结构和二维邻域关系。即使两个像素在原图中相邻，线性层也只把它们视为两个独立输入维度。CNN 通过局部卷积核编码邻域结构，Vision Transformer 则把图像划分为 patch 后建模 patch 之间的关系；这些结构更适合复杂图像任务。

MNIST 尺寸小、背景简单，MLP 已足以展示完整训练机制。它包含可学习参数、前向传播、损失函数、反向传播、梯度更新和测试集评估，但在更复杂的数据上不能据此忽略模型归纳偏置的影响。

## 3.6.11 本章小结

本节用同一个两层 MLP 完成了两套 MNIST 训练实验。NumPy 版本显式实现 mini-batch 迭代、损失反向传播和参数更新；PyTorch 版本用 `DataLoader` 管理 batch，用 `nn.Sequential` 组织网络，并由 autograd 计算梯度。

两套代码都遵循：

```text
forward -> loss -> backward -> update
```

框架能够减少样板代码，但手写梯度是否正确仍需单独验证。下一节将介绍数值梯度检查，并用有限差分结果检查解析梯度。
